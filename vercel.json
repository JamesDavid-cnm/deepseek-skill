cat > vercel.json << 'EOF'
{
  "version": 2,
  "builds": [
    { "src": "api/aligenie.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/api/aligenie", "dest": "api/aligenie.py" }
  ]
}
EOF
